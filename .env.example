# Example environment variables for local runs
PGHOST=localhost
PGPORT=5432
PGDATABASE=analytics
PGUSER=postgres
PGPASSWORD=postgres

# Optional: fallback API for customers when CSV is empty
# CUSTOMERS_API_URL=https://example.com/customers
